apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

secretGenerator:
  - name: demo-app-1-secrets
    files:
      - username=./username.txt
      - password=./password.txt
      - password2=./password2.txt

generatorOptions:
  disableNameSuffixHash: true
